# XRPL Commons hackathon MCP (Make Waves buildathon).
# Personal access token — acts as your account. NEVER commit the real value.
# Get it from https://hackathons.xrpl-commons.org and export it in your shell
# (or your local, gitignored .env). Consumed by .mcp.json -> hackathon server.
XRPL_PAT=
