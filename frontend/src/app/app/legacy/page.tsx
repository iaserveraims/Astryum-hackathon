import LegacyPanel from '../../../components/legacy/LegacyPanel';

// Astryum Legacy — XRPL governs (council + constitution + programmed
// transfers), Flare produces (the 0xFE rail into the cage of code).
// The panel is a client component; this route only mounts it.
export default function LegacyPage() {
  return <LegacyPanel />;
}
